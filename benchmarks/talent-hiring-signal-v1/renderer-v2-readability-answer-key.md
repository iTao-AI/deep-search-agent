# Renderer v2 Readability Answer Key

Open this file only after the timed independent review stops.

1. Roles: AI Agent Engineer and RAG Engineer; company: Example Company; window:
   2026-06-01 to 2026-06-09; one declared source reference.
2. Review bundle status is `required`. Snapshot eligibility is
   presentation-only: four findings are eligible and three are shown.
3. The three actually shown findings are `finding-a`, `finding-b`, and
   `finding-c`. `finding-d` is eligible but appendix-only and is not an answer
   to this question.
4. Accept either `claim-pending` because verification is `unverified` and
   review is `pending`, or `claim-conflicting` because review is `required` and
   conflict status is `conflicting`.
5. Accept one explicit item: missing `ev-missing`, pending verification for
   `ev-u`, the title contradiction, the conflicting claim, the five-evidence-
   snapshot limitation, or the prohibition on full-market extrapolation.
