## 1. 四阶段门控 Prompt 设计

- [x] 1.1 设计四阶段工作流文本（需求分析→信息收集→大纲确认→报告生成）
- [x] 1.2 定义每个阶段的准入条件、产出物和门控检查标记格式
- [x] 1.3 设计阶段完成标记格式（【阶段 N 完成】+ 关键产出摘要）

## 2. 报告大纲模板设计

- [x] 2.1 设计标准报告大纲模板（摘要→背景→核心发现→结论建议→参考资料）
- [x] 2.2 定义每个章节的内容要求和字数建议
- [x] 2.3 定义必选章节和可选章节规则

## 3. 更新 prompts.yml

- [x] 3.1 在 main_agent.system_prompt 中追加四阶段门控章节
- [x] 3.2 将报告大纲模板内嵌到 main_agent.system_prompt 的阶段 4 文本中（非独立字段）
- [x] 3.3 验证 YAML 格式正确，无语法错误

## 4. 端到端验证

- [ ] 4.1 运行一个完整任务查询，观察 Agent 是否按四阶段执行 — 静态验证已完成（prompt 结构已确认），运行时验证因 API key 缺失被阻塞
- [ ] 4.2 验证阶段完成标记是否正确输出（【阶段 N 完成】）— 静态验证已完成（标记格式已在 prompt 中定义），运行时验证因 API key 缺失被阻塞
- [ ] 4.3 验证生成的报告是否符合大纲模板结构 — 静态验证已完成（大纲模板已嵌入 prompt 阶段 4），运行时验证因 API key 缺失被阻塞
- [ ] 4.4 待 API key 就绪后执行运行时验证：观察四阶段标记实际输出顺序，验证生成的报告结构
