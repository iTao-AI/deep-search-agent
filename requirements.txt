# --- [核心依赖] Deep Agents 框架 ---
deepagents>=0.6.11           # 核心工厂方法 create_deep_agent

# --- LangChain 生态 (基础支撑) ---
langchain>=1.3.10            # LangChain 核心库
langchain-core>=1.4.8       # 基础组件 (Prompt, Message, Tool)
langchain-community>=0.4.2  # 社区工具集成
langgraph>=1.2.6            # 智能体工作流编排

# --- 大模型服务 (LLM Services) ---
langchain-openai>=1.3.2     # OpenAI 接口适配
openai>=2.43.0               # OpenAI 官方 SDK

# --- Web 服务 (API & WebSocket) ---
fastapi>=0.138.0            # 异步 Web 框架
uvicorn[standard]>=0.49.0   # 服务器引擎 (含 WebSocket)
python-multipart>=0.0.32     # 文件上传支持

# --- 数据库与存储 (Database) ---
mysql-connector-python>=8.0.0 # [修正] MySQL 官方驱动 (用于 mysql_tools)
langgraph-checkpoint-sqlite>=3.1.0
# 注意：代码中使用的是 mysql.connector，对应这个包，而非 pymysql

# --- 外部工具集成 (Integrations) ---
tavily-python>=0.7.26        # 联网搜索 API
ragflow-sdk>=0.13.0; python_version < "3.12"          # 知识库 SDK
ragflow-sdk>=0.26.0; python_version >= "3.12"
requests>=2.34.2            # HTTP 请求库

# --- 基础工具 (Utilities) ---
PyYAML>=6.0                 # [新增] YAML 配置文件解析 (prompts.py)
pydantic>=2.13.4             # 数据校验
python-dotenv>=1.2.2        # 环境变量管理
typing-extensions>=4.5.0    # 类型兼容性支持

# --- 测试依赖 (Test) ---
httpx2>=2.5.0             # Starlette TestClient supported transport
pytest>=9.0.3; python_version < "3.12"               # 单元测试框架
pytest>=9.0.3; python_version >= "3.12"
pytest-asyncio>=1.4.0      # 异步测试支持
